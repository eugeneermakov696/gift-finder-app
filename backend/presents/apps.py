from django.apps import AppConfig


class PresentsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    # Absolute path targeting the presents app module directory root
    name = "presents"
